// Package provider provides the implementation of the provider layer.
// The Provider interface lives in api/v1/provider; this package holds the
// controller, factory registry, and driver packages.
package provider
